# Sensor Data Buffer System

This project simulates an embedded system that stores sensor readings dynamically using a linked list.

## Features (Phase 1)
- Insert sensor data at beginning
- Print all stored data

## Tech Used
- C (modular structure)
- Makefile
- Git
